{
  "name": "project-root",
  "version": "1.0.0",
  "description": "Monorepo generator and sample scaffolding projects (cloud-native, REST, microservices)",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && jest"
  },
  "dependencies": {
    // Add regular dependencies required by generated scaffolds or generator here
  },
  "devDependencies": {
    "typescript": "^5.4.0", // Upgraded to TypeScript 5.4 for latest language support
    // Add and regularly update additional dev dependencies here as required
  },
  "engines": {
    "node": ">=20.0.0 <25.0.0" // Updated to reflect the latest Node.js LTS versions (20.x and 22.x) for current and near-future compatibility
  }
}