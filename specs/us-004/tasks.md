- **Repository: FRONTEND**
  - [ ] Implement validation logic in `FRONTEND/src/main.tsx` using existing libraries.
  - [ ] Add necessary error message display code to the profile form components.
  - [ ] Validate field formats dynamically as user inputs data.

- **Repository: BACKEND**
  - [ ] Integrate validation middleware in `BACKEND/src/app.ts` for the profile management routes.
  - [ ] Update `BACKEND/src/server.ts` to ensure new validation rules are enforced.
  - [ ] Write and update unit tests in `BACKEND/tests/register.test.js` to cover validation logic.

- **Testing and Documentation**
  - [ ] Create test cases in `app/tests/register.test.js` to validate user input scenarios.
  - [ ] Update `FRONTEND/guidelines/Guidelines.md` with new validation rules and messages.
  - [ ] Review and refine the code based on peer feedback.