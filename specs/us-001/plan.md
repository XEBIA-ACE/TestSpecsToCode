The delivery of this user registration story will involve several core architecture workflows already supported by the existing system. First, we'll leverage the existing `userService` for implementing user creation and registration logic. We need to ensure that the `bcrypt` hashing is implemented for user passwords and expand the email sending functionality to support registration confirmation via OTP sent through the Nodemailer email service. This might involve updating and verifying the email service configurations in the environment configuration files. We will add unit tests in `userService.test.js` to validate the new registration processes, and update the database schema if any new fields are required to accommodate email verification.